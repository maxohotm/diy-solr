#!/bin/bash -eu

source $OPENSHIFT_CARTRIDGE_SDK_BASH

case "$1" in
  -v|--version)
    version="$2"
esac


shopt -s dotglob

client_result "env"
#export JAVA_HOME=$OPENSHIFT_DIY_SOLR_JDK7

export JAVA_HOME=/etc/alternatives/java_sdk_1.7.0